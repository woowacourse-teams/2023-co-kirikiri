import styled from 'styled-components';
import {
  ModalHeader,
  ModalWrapper,
} from '@components/goalRoomDahsboardPage/goalRoomDahsboardTodo/todoModal/TodoModal.styles';

export const RoadmapModalWrapper = styled(ModalWrapper)`
  overflow-y: scroll;
  width: 80%;
  max-height: 80vh;
`;

export const RoadmapHeader = styled(ModalHeader)``;
